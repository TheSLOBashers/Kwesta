import React from "react";
import "@testing-library/jest-dom";
import { render, screen, fireEvent } from "@testing-library/react";
import CommentForm from "../src/components/CommentForm";

/*finish comment form tests, and maybe event form? 
figure out the handle click outside for both forms. 
figure out how to test scrolling for overlays
finish test cases for max branch coverage
*/
