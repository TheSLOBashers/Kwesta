{
  "name": "csc-308-app",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npm -w react-frontend run dev",
    "format": "npx prettier . --write",
    "lint": "npx eslint ."
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "nodemon": "^3.1.11"
  },
  "dependencies": {
    "mongoose": "^9.1.5",
    "prettier-airbnb-config": "^1.0.0"
  }
}
